# Utils (Legacy/Archived)

This directory contains standalone utility programs that are no longer actively developed.
They are kept for reference but may not build or run with current dependencies.

See the main `src/` directory for actively maintained code.
